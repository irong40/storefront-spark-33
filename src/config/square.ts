// Square publishable credentials (safe for frontend use)
// These are public identifiers visible in browser network requests
export const SQUARE_CONFIG = {
  applicationId: 'sq0idp-XXXXX',
  locationId: 'LXXXXX',
} as const;
